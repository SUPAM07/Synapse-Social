apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: uevent
spec:
  selector:
    app: booking-service
  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004
  type: ClusterIP
