apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: uevent
spec:
  selector:
    app: event-service
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
  type: ClusterIP
